Scope audit: delete fabricated content, fix labels to match real system

Proof section (3 -> 1):
- Deleted "7 services identified (EC2, RDS, S3, OpenAI, Stripe, Auth0, Algolia)"
  The real service list is gemini-flash/pro, stripe, sendgrid, aws-s3, vercel,
  supabase, twilio, clerk, algolia. The fabricated list was not backed by code.
- Deleted "14.2s median analysis time" — no timing instrumentation exists.
- Fixed "73% exceeded cloud budgets" -> "15% over budget on average" with the
  real Flexera 2024 URL (the old URL did not resolve, the stat was wrong).

Resources (4 -> 4, fixed URL):
- Doctavian URL: developers.doctavian.com -> developers.doctavian.com/en/get-started
  (verified to resolve via the Doctavian website's developer section link)

heroWidgets (8 -> 6):
- Deleted "Deployment Target: AWS us-east-1" (group-by widget) — the app does
  not recommend deployment targets or cloud regions. Fabricated.
- Deleted "PaaS vs Self-Managed: 73.2%" (stat-percent widget) — the app does
  not compute a PaaS ratio. Fabricated.
- Fixed cost projection tabs: ["1K","10K","50K","100K"] -> ["1K","10K","100K"]
  The cost engine projects at 3 scales, not 4.
- Fixed Service Cost Mix rows: "Compute" -> "Hosting", "Auth & CDN" -> "Payments"
  to match the real service categories (ai, payments, hosting, storage, database).
- Fixed Services Identified: value 12 -> 7 (the prompt caps at 7), chips to
  real categories, subLabel "4 architecture tiers" -> "8 service categories".
- Fixed Optimization Savings tabs: ["Compute","AI","Storage","Network"] ->
  ["Model","Caching","Tier","Arch"] to match real optimization categories
  (model-downgrade, caching, batching, tier-change, right-sizing, architecture).

Arrays left as-is (already matched real scope):
- techStack (6): all real integrations verified in code
- featureCards (3): at MVP cap, all three are real capabilities
- deepDive.primary.bullets (4): all backed by real implementation
- deepDive.sponsor.bullets (4): all backed by real Doctavian integration
- architecture.flow (4): matches the real request path
- architecture.cards (2): Xano and Nutrient, both real

Verification:
- check:placeholders passes (no new tokens)
- check:retheme passes
- npx tsc --noEmit passes
- npm run build passes (11/11 static pages, exit 0)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
